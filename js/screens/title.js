game.TitleScreen = me.ScreenObject.extend({
    /**
     *  action to perform on state changes
     */
    onResetEvent: function() {
            
    },
    
    /**
     *  action to perform when leaving this screen (state changes)
     */
    onDestroyEvent: function(){
        ;// TODO
    }
});

